package com.team3.otboo.domain.weather.enums;

public enum Category {
    // 기온 (Temperature)
    TMP,
    // 상대 습도 (Relative Humidity)
    REH,
    // 강수 형태 (Precipitation Type)
    PTY,
    // 강수 확률 (Precipitation Probability)
    POP,
    // 강수량 (Precipitation Amount)
    PCP,
    // 풍속 (Wind Speed)
    WSD,
    // 하늘 상태 (Sky Status)
    SKY,
    // 풍속 U 성분 (U–component of Wind)
    UUU,
    // 풍속 V 성분 (V–component of Wind)
    VVV,
    // 풍향 (Wind Direction)
    VEC,
    // 파고 (Wave Height)
    WAV,
    // 적설 (Snowfall)
    SNO,
    // 최저 기온 (Minimum Temperature)
    TMN,
    // 최고 기온 (Maximum Temperature)
    TMX;
}

